name: Test Docker Image CI

on:
  push:
    branches: [ "dev/new-build-system" ]
  pull_request:
    branches: [ "dev/new-build-system" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
